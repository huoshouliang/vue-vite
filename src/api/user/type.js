const loginForm = {
  username: String,
  password: String,
}
const datatype = {
  token: String,
}
const loginresponsedata = {
  code: Number,
  data: datatype,
}
export default { loginForm, loginresponsedata }
